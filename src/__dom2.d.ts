import type { selector_T } from './selector_T';
/**
 * All matching HTMLElements from the selector.
 *
 * - If selector is an object, return selector.
 */
export declare function __dom2(selector: selector_T, parent?: HTMLElement): NodeList;
